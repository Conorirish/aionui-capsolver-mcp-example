[build-system]
requires=["setuptools>=69"]
build-backend="setuptools.build_meta"
[project]
name="aionui-capsolver-mcp-example"
version="0.1.0"
description="AionUI stdio MCP configuration with bounded recovery."
readme="README.md"
requires-python=">=3.11"
dependencies=[]
[tool.setuptools.packages.find]
where=["src"]
